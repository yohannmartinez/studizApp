import "./buttonLoader.scss";

/**
 * Button with loading inside
 */
const ButtonLoader = ({ text, isLoading }) => {
  return (
    <button className="searchResults__noMoreResults__button">
      Charger d'avantage
    </button>
  );
};

export default ButtonLoader;
